syntax = "proto3";

package dfuse.cursors;

option go_package = "cursors";

message TransactionCursor {
  uint32 version = 1;
  uint64 block_num = 2;
  bytes block_id = 3;

  bytes transaction_hash_prefix = 5;
  uint64 transaction_index_within_block = 6;
}
