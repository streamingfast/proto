syntax = "proto3";

package dfuse.devproxy.v1;

option go_package = "devproxy";

service Devproxy {
  rpc ListServers (ListRequest) returns (ListResponse);
}

message ListRequest {}
message ListResponse {
  repeated string servers = 1;
}
